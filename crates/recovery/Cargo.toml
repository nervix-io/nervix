[package]
description = "The recovery class: the readings a caller may give a failure it has already survived"
edition.workspace = true
license-file.workspace = true
name = "nervix-recovery"
version.workspace = true

[lints]
workspace = true

[dependencies]
tracing = { workspace = true }

[dev-dependencies]
tracing-subscriber = { workspace = true, features = ["fmt"] }
