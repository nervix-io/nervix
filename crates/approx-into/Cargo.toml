[package]
description = "Rounding and checked conversions between integers and floating point, for the pairs `From` and `TryFrom` cannot express"
edition.workspace = true
license-file.workspace = true
name = "nervix-approx-into"
version.workspace = true

[lints]
workspace = true
