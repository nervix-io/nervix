# Installation

TBD. Packaged installation instructions — released binaries and container images — are not yet
published.

Until they are, build and run Nervix from a repository clone as described in
[Developing Nervix](developing-nervix.md). The rest of this quickstart writes commands as
`nervix-server` and `nervix-cli`; from a clone, run the same commands as
`just server -- <flags>` and `just client -- <flags>`.
