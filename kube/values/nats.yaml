config:
  cluster:
    enabled: true
    replicas: 3
  jetstream:
    enabled: false
  nats:
    port: 4222

container:
  merge:
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        memory: 256Mi
