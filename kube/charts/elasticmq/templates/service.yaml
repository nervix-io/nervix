apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticmq.fullname" . }}
  labels:
    {{- include "elasticmq.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "elasticmq.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
