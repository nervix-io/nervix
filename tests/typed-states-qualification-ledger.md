# Typed states qualification ledger

This is the acceptance record for [Typed States 10](https://app.clickup.com/t/86bc2gn70).
It reviews the confirmed findings from the [typed states epic](https://app.clickup.com/t/86bc2gm9d)
against `origin/main` at `c12a2cc2`. The cited commits are the landed owning changes; the
linked source and tests describe the current state. The audit follows each semantic path from
construction through execution or reporting. A numeric literal alone does not establish that a
value encodes absence.

| Confirmed finding | Owner and landed change | Current semantic path and test evidence |
| --- | --- | --- |
| Expression scopes invented names; a display label selected the message-error operation | VM frontend, registry and runtime: [scope policy #346](https://github.com/nervix-io/nervix/pull/346), with the typed error carrier in `974828e4` | [Frontend](../crates/nervix-vm/src/frontend.rs), [message-error Model](../crates/models/src/message_error.rs) and [runtime operation tests](../src/runtime/filter_map/tests/typed_operation_tests.rs) carry scope availability and `MessageErrorOperation` as types. [Branch namespaces](features/runtime/branch_namespace.feature), [expression semantics](features/runtime/expression_function_semantics.feature) and [error policies](features/runtime/node_error_policies.feature) exercise valid scopes, unavailable scopes and structured route operations. |
| Empty branch fields represented unbranched execution; branch sensitivity could diverge from its schema; reporting formatted branch absence too early; ASCII graph rendering supplied an empty domain | Registry, scheduled Models, runtime and observation: [resolved branch identity #347](https://github.com/nervix-io/nervix/pull/347) | [ResolvedBranching](../crates/models/src/statement.rs) carries either unbranched state or the named branch with its fields and sensitivity. [Graph projection](../src/registry/graph.rs) does not invent a domain. [Explicit branches](features/runtime/explicit_branch.feature) distinguish equal key shapes with different names; [materialized state](features/runtime/materialized_stream.feature) checks root reporting locally and remotely and interleaved concrete branches; [branched subscriptions](features/runtime/branched_subscription.feature) check sensitivity. |
| All-zero fingerprints conflated absent identity and schema-independent state | Registry scheduling, runtime state store and interconnect: [state identity #354](https://github.com/nervix-io/nervix/pull/354) | [SchemaFingerprint](../crates/models/src/schema_fingerprint.rs) is required for schema-bound scheduled nodes, while [runtime state identity](../src/runtime/state_store.rs) explicitly distinguishes schema-independent state. [Materialized state schema change](features/runtime/relay_materialized_state_alter.feature) and owning state-store tests cover recreation, restart and stale state rejection. |
| Maximum revision meant never applied; snapshot zero meant no completed snapshot | Runtime schedule application and consensus retention: [optional progress #342](https://github.com/nervix-io/nervix/pull/342) | [Schedule application](../src/runtime/schedule_apply.rs) stores an optional applied revision under its existing mutex and tests zero, equal, stale, maximum and failed application. [Retention](../crates/consensus/src/retention.rs) distinguishes no request, a request before any completed snapshot and completed index zero. [Narrow activation](features/cluster/narrow_schedule_activation.feature) and [consensus storage](features/cluster/consensus_storage.feature) exercise the public recovery boundaries. |
| ACK handoff completion was a special share count | Runtime ACK tracker: [typed handoff #341](https://github.com/nervix-io/nervix/pull/341), [attachment race #332](https://github.com/nervix-io/nervix/pull/332) | [AckHandoffState](../src/runtime_ack.rs) exposes tracking and completion while its owner alone encodes them in one atomic word. The same file tests active attachment, final ACK, `REQUIRED WAIT`, and domain/ingestor tracker conservation under Shuttle; [materialized state](features/runtime/materialized_stream.feature) and [narrow activation](features/cluster/narrow_schedule_activation.feature) cross the public waiting and handoff paths. |
| Missing gossip endpoints became empty strings and could enter admission | Gossip publication, consensus membership and peer health: [endpoint availability #343](https://github.com/nervix-io/nervix/pull/343) | [NodeEndpoint](../crates/models/src/node_endpoint.rs), [gossip publication](../src/cluster.rs), [consensus admission](../crates/consensus/src/lib.rs) and [peer targets](../crates/interconnect/src/peer_target.rs) retain endpoint availability. Unit tests cover field-by-field publication, malformed or replaced endpoints and absent interconnect admission. [Bootstrap](features/cluster/bootstrap.feature), [rejoin](features/cluster/rejoin.feature), [leader failover](features/cluster/leader_failover.feature) and [session redirect](features/runtime/session_protocol.feature) exercise observable transitions. |
| BSON conversion failure became null, including for conflict keys | MongoDB connector: [per-record rejection #380](https://github.com/nervix-io/nervix/pull/380) | [MongoDB conversion](../crates/connectors/mongodb/src/lib.rs) produces a typed record failure without publishing the document; its tests cover top-level and nested unsigned values. [MongoDB emission](features/runtime/mongodb_emission.feature) checks ordinary fields, conflict keys, genuine null and mixed valid records on one and three nodes. |
| VM count arguments narrowed unsigned integers; unsupported UUID time became epoch time | VM execution: [range handling #389](https://github.com/nervix-io/nervix/pull/389) | [Count values](../crates/nervix-vm/src/count.rs) preserve integer signedness and extremes; [VM execution](../crates/nervix-vm/src/runtime.rs) reports unsupported output and pre-epoch UUID time as row errors. [Expression semantics](features/runtime/expression_function_semantics.feature) checks full unsigned counts and output bounds; [domain clock](features/runtime/domain_clock_contract.feature) checks epoch and pre-epoch UUID time. |
| Session adapters substituted zero or empty text for absent metadata and routed replies by message text | Session, Client Wire, client core and browser: [typed resource outcomes #435](https://github.com/nervix-io/nervix/pull/435), over the [FlatBuffers session cutover #394](https://github.com/nervix-io/nervix/pull/394) | [Upload outcome](../src/application/session/upload.rs) retains an optional nonzero assigned version; [wire diagnostics](../crates/client-wire/src/common.rs) retain optional spans, including a span starting at zero; [console dispatch](../crates/web-console/src/main.rs) uses typed command purpose and outcome. [Wire tests](../crates/client-wire/src/tests/replies.rs), [client tests](../crates/client-core/src/tests.rs), [session protocol](features/runtime/session_protocol.feature) and [console REPL](features/web-console/nspl_repl.feature) cover absent and present metadata, typed redirects and domain selection. |

The audit retained genuine zero counts and initial sequence values, empty content, Arrow null
lanes under validity masks, the domain clock's atomic maximum identity, documented scalar
function results, and external or packed encodings that are private to their typed owners. No
additional representation defect was found in these confirmed paths. There is no blanket
sentinel ratchet because a source token cannot distinguish these valid values from absent state.

## Verification

All nine prerequisite ClickUp tasks were Complete before this review. No product code or public
contract changes in this qualification, so its public evidence is the existing Cucumber scenarios
rather than a new scenario. The table records the scenario names, owning tests and local
qualification results.

| Command | Result |
| --- | --- |
| `just test-scenarios --input <feature>` for `branch_namespace`, `explicit_branch`, `materialized_stream`, and `branched_subscription` in `tests/features/runtime` | 36 scenarios, 378 steps passed; every feature includes one-node and three-node examples where applicable. |
| `just test-scenarios --input <feature>` for `node_error_policies`, `expression_function_semantics`, `relay_materialized_state_alter`, `mongodb_emission`, and `domain_clock_contract` in `tests/features/runtime` | 84 scenarios, 791 steps passed; the MongoDB and domain-clock outlines exercised both topologies. |
| `just test-scenarios --input <feature>` for `bootstrap`, `rejoin`, `leader_failover`, `consensus_storage`, and `narrow_schedule_activation` in `tests/features/cluster`, then `session_protocol` in `tests/features/runtime` and `nspl_repl` in `tests/features/web-console` | 97 scenarios, 1,615 steps passed, including production sticky ownership scenarios and the browser session path. |
| `just test-package-lib nervix-models`, `just test-vm`, `just test-consensus`, `just test-interconnect`, `just test-package-lib nervix-connector-mongodb`, `just test-client-wire`, `just test-package-lib nervix-client-core`, `just test-package-bins nervix-web-console`, `just test-lib` | 2,399 tests passed: 198 Models, 552 VM, 104 consensus, 44 interconnect, 6 MongoDB, 122 Client Wire, 89 client core, 59 browser, and 1,225 server. |
| `just test-shuttle` | 70 distinct checks passed under ordinary and nondeterminism-detection schedules (140 executions), including ACK handoff and state recovery races. |
| `just coverage-lib target/typed-states-vm-coverage.lcov --package nervix-vm` | Passed 552 instrumented VM tests and wrote LCOV. The only changed file is this Markdown ledger, so patch coverage has zero executable lines and the 80% threshold is not applicable. |
| `just validate` | Passed, including format, lint, public NSPL skill, documented NSPL, clock boundaries, typed errors and dependency checks. |
| `just ratchet` | Passed at `c12a2cc2`; no count increased or decreased, so the checked-in baseline needs no update. |

In total, the focused public selection passed 217 scenarios and 2,784 steps. The one-node and
three-node scenario outlines use their declared topology examples. Ownership and recovery
scenarios in the selection configure the production sticky scheduler where their subject requires
it.
